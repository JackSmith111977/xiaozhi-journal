# Epic 3 Retrospective

Date: 2026-04-26
Epic: 3 — AI 温暖回应
Status: done

## Epic Summary

4 stories completed. 16 rounds of code review. 26 patch items fixed (2H, 11M, 13L). 22 items deferred. Zero production incidents. Zero console errors in E2E.

## Participants

- Kei (Project Lead)
- Amelia (Developer)
- Charlie (Senior Dev)
- Dana (QA)
- Winston (Architect)

## Successes

1. **双模式 AI 管道落地** — 平台 Key + BYOK 分支逻辑，Route Handler 实现
2. **Fallback 机制完善** — 15s timeout + fallback quotes + `fromFallback` 标记
3. **打字机 + 金句动画序列** — onComplete callback + 300ms delay 实现 UX-DR9/DR8
4. **离线 pending 处理** — IndexedDB → syncPendingWithAI 自动回调
5. **E2E 验证首次运行** — Playwright + Next.js MCP 自动化测试，Happy Path + 认证检查通过

## Challenges

1. **Review 轮次偏高** — 16 轮（Epic 2: 4 轮），技术难度显著提升
2. **Null/Undefined 保护缺失** — 5 次出现（route, encryption, keyData, timestamp）
3. **Race Condition/Timing** — 4 次出现（onComplete timer, syncing, online event）
4. **BYOK 前端状态同步遗漏** — journal-input 未发送 `useByok` 参数
5. **Store 状态未订阅渲染** — syncProgress 定义但 page.tsx 未渲染
6. **Deferred 项累积** — 22 项，无后续处理计划

## Epic 2 Action Items Follow-through

| Action | 状态 | 说明 |
|--------|------|------|
| AI调用提取到Route Handler | ✅ 完成 | `/api/journal` 实现 |
| AnimatePresence最小验证 | ⚠️ 部分 | 3-3仍有4个动画修复项 |
| useRef防抖gate | ⚠️ 未完全 | 3-3/3-4仍有timing bug |

## Pattern Analysis

| Pattern | 出现次数 | Stories |
|---------|---------|---------|
| Null/Undefined 缺保护 | 5 | 3-1, 3-2, 3-4 |
| Race Condition/Timing | 4 | 3-3, 3-4 |
| Error Handling/Fetch | 4 | 3-1, 3-4 |
| 状态同步问题 | 3 | 3-2, 3-4 |

## Key Insights

1. **Route Handler ≠ 前端状态自动同步** — API 改了，前端调用方式也要改
2. **Timer/useEffect cleanup 是第一公民** — Epic 2 Action Item 3 部分执行，但仍出现 timing bug
3. **Store 状态 ≠ UI 渲染** — syncProgress 定义了，page.tsx 没订阅。状态和渲染要绑定检查
4. **deferred 项需要有 owner 和 timeline** — 目前只标记 deferred，无后续处理计划

## Deferred Items

### 推迟至后续 Story（5项）
- 频率限制 → Story 10-2
- 数据库持久化 → Story 10-2
- remainingCalls 字段 → Story 10-2
- AI Usage 端点 → Story 10-2
- Race condition: journal update mid-typewriter → 需统一重构

### Pre-existing MVP 可接受（7项）
- pendingMessage 幽灵状态 — store定义但未使用
- syncing 双重重置 — 多处设置 syncing=false
- timestamp 无效日期 — new Date可能 Invalid
- 网络中断无检查 — 循环无 navigator.onLine
- useAppStore 非 React context — getState() 外部调用
- theme tokens vs spec colors — 依赖 Tailwind 匹配
- Key enumeration timing — MVP 风险低

### 已处理/可接受（10项）
- API Key 内存未清除 — JS string immutable
- Rate Limiting 缺失 → 10-2
- CSRF 保护 — Next.js 已处理
- Upsert 并发 — DB unique constraint
- Content-Type — Next.js 自动处理
- Auth Pattern 不一致 — 样式差异
- 时钟偏差 — MVP 可接受
- 环境变量延迟 panic — 运行时校验足够
- invalidKey 字段不一致 — 前端按场景处理
- BYOK 移除 — 已在 3-2 实现

## Action Items

| # | Action | Owner | 期限 | 完成条件 |
|---|--------|-------|------|---------|
| 1 | Timer/useEffect cleanup规范 — 每个timer必须有cleanup return | Amelia | Epic 4前 | 新story无unmount timing bug |
| 2 | Store状态变更时检查订阅组件 — 状态定义后强制检查哪些组件需要订阅 | Amelia | Epic 4开发时 | 每个store状态有订阅清单 |
| 3 | Deferred项清理 — pendingMessage/syncing重置处理 | Amelia | Epic 4前 | deferred减少到<5 |
| 4 | BYOK前端修复 — journal-input发送useByok参数 + E2E覆盖 | Amelia | 本周 | BYOK模式E2E测试通过 |

## Review Statistics

| Story | Rounds | Patch Items | Defer Items |
|-------|--------|-------------|-------------|
| 3-1 | 4 | 6 | 5 |
| 3-2 | 5 | 11 | 10 |
| 3-3 | 4 | 4 | 2 |
| 3-4 | 3 | 5 | 5 |
| **Total** | **16** | **26** | **22** |

## Next Epic Preview: Epic 4 — 情绪波形图

2 stories: SVG波形图 + Hover Tooltip。95% 复用现有代码。依赖 Epic 3 的 AI 回应后波形图更新已验证。

## E2E Verification

- Happy Path: ✅ AI 响应 + 金句 + 波形图更新
- 认证检查: ✅ 401 未授权
- BYOK无效Key: ✅ "API Key 无效，请检查"
- 离线场景: ⚠️ 需手动测试（Playwright无法模拟断网）